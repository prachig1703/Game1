<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#050505">

    <!-- 1. The Logo (Starts large in the absolute center) -->
    <com.example.game1.AnimatedLogoView
        android:id="@+id/logoView"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_centerInParent="true" />

    <!-- 2. The Title (Staged slightly below center, invisible at start) -->
    <TextView
        android:id="@+id/textLogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dp"
        android:alpha="0"
        android:shadowColor="#00FFFF"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="30"
        android:text="NEON\nTIC TAC TOE"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:textStyle="bold" />

    <!-- 3. The Start Button (Staged below the Title, invisible at start) -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnStart"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_below="@id/textLogo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:alpha="0"
        android:background="@drawable/round_option_button"
        android:text="START GAME"
        android:textColor="#39FF14"
        android:textSize="20sp"
        android:textStyle="bold" />

</RelativeLayout>